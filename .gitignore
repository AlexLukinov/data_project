# Secrets (real ones — lab secrets in infra/secrets.yaml are intentionally committed)
.env
*.env
!.env.example

# Python
__pycache__/
*.pyc
.venv/
venv/

# dbt
dbt/shop_dwh/target/
dbt/shop_dwh/dbt_packages/
dbt/shop_dwh/logs/
.user.yml

# OS / editor
.DS_Store
*.swp
.idea/
.vscode/

# Local kube/helm junk
*.tmp
kubeconfig

# Real hand-history exports — NEVER commit. They contain other players' screen names and
# betting behaviour: third-party personal data (GDPR / 152-ФЗ). Synthetic corpus for tests
# lives in platform/seeds/hands/ and is safe.
*.zip
*_HH_*
*-HH-*
hand_histories/

# Proprietary solver/range binaries (e.g. Simple Preflop Holdem exports) — kept locally for importer R&D
*.bin

# Session scratch: per-lane file manifests and throwaway scripts from parallel sessions.
# Deliberately untracked — they describe one session's working tree, not the repo.
scratchpad/

# The founder's own Claude Code configuration. Never staged.
.claude/
