-- GENERATED by `make gen` from stats/registry -- edit the registry, not this file.
-- The materialized-view half of the rollup (ADR-003, ADR-025, ADR-044): a target table
-- shaped exactly like `marts.stats_daily`, and the boundary that makes a forward-only
-- view safe to create on a populated table.
--
-- The MV does NOT write into `marts.stats_daily`, and that is the whole point: that
-- table is the incremental chain's anchor, and a second writer in it makes the
-- dirty-partition gate compare a value against itself. See stats_daily_mv in
-- scripts/gen_stats.py for the failure in full.

CREATE TABLE IF NOT EXISTS _meta.mv_boundaries (
    view String,
    source String,
    boundary_at DateTime64(3, 'UTC'),
    created_at DateTime64(3, 'UTC') DEFAULT now64(3)
) ENGINE = ReplacingMergeTree(created_at) ORDER BY view;

CREATE TABLE IF NOT EXISTS marts.stats_daily_mv (
    user_id UInt32,
    dataset LowCardinality(String),
    player_key String,
    day Date,
    site LowCardinality(String),
    stake_level LowCardinality(String),
    game_type LowCardinality(String),
    table_format LowCardinality(String),
    position LowCardinality(String),
    is_hero UInt8,
    is_anonymized UInt8,
    hands UInt64,
    bb_per_100_opp UInt64,
    bb_per_100_action Decimal(38, 4),
    ev_bb_per_100_opp UInt64,
    ev_bb_per_100_action Decimal(38, 4),
    sd_bb_per_100_opp UInt64,
    sd_bb_per_100_action Decimal(38, 4),
    nsd_bb_per_100_opp UInt64,
    nsd_bb_per_100_action Decimal(38, 4),
    cbet_flop_opp UInt64,
    cbet_flop_action UInt64,
    cbet_turn_opp UInt64,
    cbet_turn_action UInt64,
    cbet_river_opp UInt64,
    cbet_river_action UInt64,
    delayed_cbet_opp UInt64,
    delayed_cbet_action UInt64,
    fold_to_cbet_flop_opp UInt64,
    fold_to_cbet_flop_action UInt64,
    call_cbet_flop_opp UInt64,
    call_cbet_flop_action UInt64,
    raise_cbet_flop_opp UInt64,
    raise_cbet_flop_action UInt64,
    fold_to_cbet_turn_opp UInt64,
    fold_to_cbet_turn_action UInt64,
    fold_to_cbet_river_opp UInt64,
    fold_to_cbet_river_action UInt64,
    fold_to_delayed_cbet_opp UInt64,
    fold_to_delayed_cbet_action UInt64,
    float_flop_opp UInt64,
    float_flop_action UInt64,
    float_fold_opp UInt64,
    float_fold_action UInt64,
    donk_flop_opp UInt64,
    donk_flop_action UInt64,
    fold_to_donk_opp UInt64,
    fold_to_donk_action UInt64,
    probe_turn_opp UInt64,
    probe_turn_action UInt64,
    fold_to_probe_turn_opp UInt64,
    fold_to_probe_turn_action UInt64,
    probe_river_opp UInt64,
    probe_river_action UInt64,
    fold_to_probe_river_opp UInt64,
    fold_to_probe_river_action UInt64,
    check_raise_flop_opp UInt64,
    check_raise_flop_action UInt64,
    check_raise_turn_opp UInt64,
    check_raise_turn_action UInt64,
    check_raise_river_opp UInt64,
    check_raise_river_action UInt64,
    fold_to_flop_raise_opp UInt64,
    fold_to_flop_raise_action UInt64,
    fold_to_turn_raise_opp UInt64,
    fold_to_turn_raise_action UInt64,
    fold_to_river_raise_opp UInt64,
    fold_to_river_raise_action UInt64,
    bet_call_river_opp UInt64,
    bet_call_river_action UInt64,
    river_raise_opp UInt64,
    river_raise_action UInt64,
    river_check_fold_opp UInt64,
    river_check_fold_action UInt64,
    river_check_call_opp UInt64,
    river_check_call_action UInt64,
    vpip_opp UInt64,
    vpip_action UInt64,
    pfr_opp UInt64,
    pfr_action UInt64,
    rfi_opp UInt64,
    rfi_action UInt64,
    steal_opp UInt64,
    steal_action UInt64,
    limp_opp UInt64,
    limp_action UInt64,
    iso_raise_opp UInt64,
    iso_raise_action UInt64,
    cold_call_opp UInt64,
    cold_call_action UInt64,
    defend_vs_open_opp UInt64,
    defend_vs_open_action UInt64,
    fold_vs_open_opp UInt64,
    fold_vs_open_action UInt64,
    threebet_opp UInt64,
    threebet_action UInt64,
    squeeze_opp UInt64,
    squeeze_action UInt64,
    fold_to_3bet_opp UInt64,
    fold_to_3bet_action UInt64,
    call_3bet_opp UInt64,
    call_3bet_action UInt64,
    fourbet_opp UInt64,
    fourbet_action UInt64,
    fold_to_4bet_opp UInt64,
    fold_to_4bet_action UInt64,
    call_4bet_opp UInt64,
    call_4bet_action UInt64,
    fivebet_opp UInt64,
    fivebet_action UInt64,
    fold_bb_to_steal_opp UInt64,
    fold_bb_to_steal_action UInt64,
    limp_fold_opp UInt64,
    limp_fold_action UInt64,
    limp_call_opp UInt64,
    limp_call_action UInt64,
    limp_raise_opp UInt64,
    limp_raise_action UInt64,
    wwsf_opp UInt64,
    wwsf_action UInt64,
    wtsd_opp UInt64,
    wtsd_action UInt64,
    wsd_opp UInt64,
    wsd_action UInt64,
    src_parsed_at DateTime64(3, 'UTC')
) ENGINE = SummingMergeTree()
PARTITION BY toYYYYMMDD(day)
ORDER BY (user_id, dataset, player_key, day, site, stake_level, game_type, table_format, position, is_hero, is_anonymized);
